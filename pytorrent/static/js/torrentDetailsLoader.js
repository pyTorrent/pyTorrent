export const torrentDetailsLoaderSource = "  async function loadDetails(tab, options={}){\n    const hash=selectedHash;\n    const t=torrents.get(hash);\n    const silent = !!options.silent;\n    const pane=$('detailPane');\n    if(tab !== 'files') clearFilesAutoRefresh();\n    if(tab !== 'peers') clearReverseDnsPeerRefresh();\n    const peersRefreshBox=$('peersRefreshBox');\n    if(peersRefreshBox){\n      const peersRefreshHidden = tab !== 'peers';\n      // Note: Keep the peers control in layout while hidden so switching to Peers never changes the detail tabbar height.\n      peersRefreshBox.classList.toggle('invisible', peersRefreshHidden);\n      peersRefreshBox.setAttribute('aria-hidden', peersRefreshHidden ? 'true' : 'false');\n    }\n    if(typeof renderDetailSummary==='function') renderDetailSummary();\n    if(!t){\n      setupPeersRefresh('');\n      clearFilesAutoRefresh();\n      clearReverseDnsPeerRefresh();\n      // Note: During rTorrent startup keep the explicit waiting state. Once a valid snapshot exists, replace stale startup text even for an empty profile.\n      if(hasTorrentSnapshot && pane){\n        delete pane.dataset.rtorrentStarting;\n        pane.innerHTML=emptyStateMarkup('Select a torrent', 'Choose a torrent from the list to view its details.', 'fa-circle-info');\n      }\n      return;\n    }\n    if(pane) delete pane.dataset.rtorrentStarting;\n    setupPeersRefresh(tab);\n    if(tab==='general') return renderGeneral();\n    if(tab==='log'){\n      // Note: The Log tab uses the same torrent message field as General and renders it as normal readable text.\n      $('detailPane').innerHTML=t.message?`<div class=\"torrent-log-message\">${esc(t.message)}</div>`:emptyStateMarkup('No torrent log message.', 'rTorrent has not reported a message for this torrent.', 'fa-terminal');\n      return;\n    }\n    if(tab==='files' && filesViewMode==='tree'){\n      clearFilesAutoRefresh();\n      return renderFileTreeView();\n    }\n    if(!silent) pane.innerHTML=`<div class=\"loading-line\"><span class=\"spinner-border spinner-border-sm\"></span> Loading ${esc(tab)}...</div>`;\n    try{\n      // Note: Background peer refresh keeps the current table visible and only swaps in fresh rows after a successful response.\n      const detailUrl = tab==='chunks' ? `/api/torrents/${encodeURIComponent(hash)}/chunks?max_cells=${chunkMaxCellsForDensity()}` : `/api/torrents/${encodeURIComponent(hash)}/${tab}`;\n      const res=await fetch(detailUrl,{headers:{'Accept':'application/json'}});\n      const text=await res.text();\n      let json;\n      try{\n        json=JSON.parse(text);\n      }catch(parseErr){\n        throw new Error(`Invalid API response for ${tab}. HTTP ${res.status}`);\n      }\n      if(!res.ok || !json.ok) throw new Error(json.error||`HTTP ${res.status}`);\n      if(tab!==activeTab() || selectedHash!==hash) return;\n      if(tab==='files'){ if(filesViewMode!=='list') return; renderFiles(json.files||[]); }\n      if(tab==='chunks') renderChunks(json.chunks||{});\n      if(tab==='peers') renderPeers(json.peers||[]);\n      if(tab==='trackers') renderTrackers(json.trackers||[]);\n    }catch(e){\n      if(!silent) pane.innerHTML=`<div class=\"detail-error-state\">${emptyStateMarkup('Could not load details.', e.message, 'fa-triangle-exclamation')}</div>`;\n    }\n  }\n";
