export const mobileSelectEventsSource = "  document.addEventListener('change',e=>{\n    const filter=e.target.closest('#mobileFilterSelect');\n    if(filter){ setMobileFilterValue(filter.value); return; }\n    const sort=e.target.closest('#mobileSortSelect');\n    // Note: Direct mobile sort selection uses the same complete step list as the quick-sort cycle button.\n    if(sort) setMobileSortValue(sort.value);\n  });\n  ";
